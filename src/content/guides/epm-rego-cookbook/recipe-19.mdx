import { CodeBlock, RemoteCodeBlock } from '@/components/CodeBlock';

# Recipe 19: Enforce Known Vendor Prefix 

Ensure packages come from vendors with recognized naming prefixes.

<RemoteCodeBlock
  lang="rego"
  src="https://raw.githubusercontent.com/cloudsmith-io/rego-recipes/refs/heads/main/recipe-19/policy.rego"
/>

A successful policy creation should look like this:

![][image23]

## Set Policy Actions

After a policy is created, actions can be assigned to it via the [**workspaces_policies_actions_create**](https://api.cloudsmith.io/v2/redoc/#tag/workspaces/operation/workspaces_policies_actions_create) REST API method.

In this cookbook, two actions can be added to your policies:

1. An action to quarantine a package matched by the matching logic.  
2. An action to tag a package matched by the matching logic.

### Quarantine Action 

```
curl -X POST "https://api.cloudsmith.io/v2/workspaces/{workspace-name}/policies/{POLICY_SLUG}/actions/"   
  -H "Content-Type: application/json"   
  -H "X-Api-Key: $CLOUDSMITH_API_KEY"   
  -d '{  
    "action_type": "SetPackageState",  
    "precedence": 1,  
    "package_state": "QUARANTINED"  
  }'   | jq .
```

### Tagging Actions 

```
curl -X POST "https://api.cloudsmith.io/v2/workspaces/{workspace-name}/policies/{POLICY_SLUG}/actions/"   
  -H "Content-Type: application/json"   
  -H "X-Api-Key: $CLOUDSMITH_API_KEY"   
  -d '{  
    "action_type": "AddPackageTags",  
    "precedence": 2,  
    "tags": ["policy-violated"]  
  }'   | jq .
```