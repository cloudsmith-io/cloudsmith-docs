import { Method } from '@/components/Method';

```js
function isJSONString(message) {
  try {
    // Check if the message is a valid JSON string by parsing it and checking if it's an object or not
    const json = JSON.parse(message);
    return typeof json === 'object';
  } catch (e) {
    return false;
  }
}
```

```text
npx create-next-app
```

```ts
console.log('Not focused');
console.log('Focused');
console.log('Not focused');
```

```js
import { Method } from '@/components';

function Test() => {
  const test = 123; // [!code highlight]
  return test;
}
```

# GFM

## Autolink literals

www.example.com, https://example.com, and contact@example.com.

## Footnote

A note[^1]

[^1]: Big note.

## Strikethrough

~one~ or ~~two~~ tildes.

## Table

| a   | b   |   c |  d  |
| --- | :-- | --: | :-: |

## Tasklist

- [ ] to do
- [x] done done test

<Method type="get">get</Method>
