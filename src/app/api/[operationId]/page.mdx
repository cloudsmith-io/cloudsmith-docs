import { BlockImage, Method } from '@/components';

import Test from './test.mdx';

### Local image mdx

![Local image example](/temp/temp.png 'Local image')

### External html img tag

<img src="https://github.com/favicon.ico" width="48" />

### Local image mdx

<BlockImage variant="base" src="/temp/temp.png" width={136} height={136} alt="No text" />
<BlockImage variant="base" src="/temp/temp.png" width={136} height={136} alt="No text">
  Align center. Jorem ipsum dolor sit amet, consectetur adipiscing elit. Nunc vulputate libero et velit
  interdum, ac aliquet odio mattis.
</BlockImage>
<BlockImage variant="base" align="left" src="/temp/temp.png" width={136} height={136} alt="No text">
  align left
</BlockImage>
<BlockImage variant="base" align="right" src="/temp/temp.png" width={136} height={136} alt="No text">
  <Test />
</BlockImage>

```js
function isJSONString(message) {
  try {
    // Check if the message is a valid JSON string by parsing it and checking if it's an object or not
    const json = JSON.parse(message);
    return typeof json === 'object';
  } catch (e) {
    return false;
  }
}
```

```text
npx create-next-app
```

```ts
console.log('Not focused');
console.log('Focused');
console.log('Not focused');
```

```js
import { Method } from '@/components';

function Test() => {
  const test = 123; // [!code highlight]
  return test;
}
```

[MDX](https://mdxjs.com 'title')

> A greater than…

---

# GFM

## Autolink literals

www.example.com, https://example.com, and contact@example.com.

## Footnote

A note[^1]

[^1]: Big note.

## Strikethrough

~one~ or ~~two~~ tildes.

## Table

| a   | b   |   c |  d  |
| --- | :-- | --: | :-: |

## Tasklist

- [ ] to do
- [x] done

<Method type="get">get</Method>
